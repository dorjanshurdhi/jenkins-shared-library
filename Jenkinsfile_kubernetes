@Library('jenkins-shared-library') _

pipeline{
    agent {
        node {
            label 'master'
        }
    }
    stages{
        stage('Test Shared Libs'){
            steps{
                script {
                    def configMap = [
                       'kubernetes-partner-name':'partner-2',
                       'serverUrl': 'https://kubernetes.example.com',
                       'namespace': 'devops-tools',
                       'credentialsId': 'my-credentials-id',
                       'podTemplate': [
                           'label': 'kubeagent',
                           'name': 'kubeaagent',
                           'namespace': 'devops-tools'
                        ]
                    ]
                    kubernetes(configMap)                
                }
            }
        }
    }
}